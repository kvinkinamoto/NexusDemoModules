<div>
    <p>One row per user+product pair. Scoped to products only — see the model docblock.</p>
    <p>Frontend API: <code>GET/POST /api/wishlist</code> (authenticated add/remove/sync/list) and <code>POST /api/wishlist/resolve</code> (guest-allowed, resolves a locally-stored list of product ids to full product details).</p>
</div>
